# 果园物候图谱编研台

## 项目目标

本项目建设一个面向地方品种保护人员、果园档案员和农业文化研究者的全栈编研工具。用户可以从园区建档开始，逐株登记果树，按季节记录芽膨大、开花、坐果、成熟和落叶等物候阶段，再把两份已完成的季节志进行对齐比较，形成可长期保存的品种物候图谱与编研简报。

系统只使用自身保存的结构化档案，不连接外部气象、地图或农业服务。所有日期、阶段和比较结果都可以在本机复现。

## 使用者与职责

- 档案员：建立园区与植株编目，补充地方名称、传统称呼和管理说明。
- 观察员：在指定季节内补录物候阶段、观察日期、置信度和现场文字。
- 编研人员：比较不同植株或品种，核对日期偏移，形成只读简报。

当前基线采用单机档案，不包含多人账户或远程身份服务。观察者身份以字符串署名记录。

## 核心实体

### 园区

包含园区编号、名称、地点描述、重点品种、管护机构或责任人、起始种植年份、备注、状态和修订号。状态只允许从 `draft` 转为 `confirmed`，确认后基础信息只读；如需修订，必须新建下一版园区记录。

### 植株

属于一个园区，包含植株编号、品种名、砧木名、定植年份、生长状态、备注、修订号。生长状态为 `active`、`retired` 或 `lost`。编号在同一园区内不可重复，已退休或遗失的植株保留历史观察。

### 季节志

针对一株树和一个年份建立。状态为 `open` 或 `completed`。草拟季节志可以调整阶段条目，完成时必须满足进度规则，完成后不可增删条目。

### 物候条目

阶段、观察日期、置信度、文字说明。同一季节志内一个阶段只能出现一次。阶段日期必须随既定物候顺序单调不倒退，并落在季节年份附近允许的观察窗口内。

### 对比图谱

固定两份同一年份的已完成季节志，只使用两边都有记录的阶段计算日期偏移。结果包含共同阶段、左右日期、偏移天数、共同阶段数量和结论摘要。生成后保持不可变。

### 多年比较

针对同一植株和一个连续年份范围建立。范围内每个年份被明确区分为纳入（季节志已完成）、排除（季节志未完成）或缺失（无季节志记录），并记录机器可读的原因码与可读原因。阶段平均、年际偏移等统计只基于纳入年份计算：缺失年份不按零值处理，排除年份不参与平均，纳入年份中缺少的阶段按阶段剔除且不补零、不推断日期。结果包含纳入标准、逐年处置、分阶段序列和结论摘要，生成后保持不可变，并可导出文本。

### 编研简报

基于已确认园区生成，冻结园区信息、植株清单、已完成季节志摘要和生成时的数据修订号。后续修改不会改变既有简报。

## 物候阶段

系统使用固定的九个阶段，顺序如下：

1. `bud_swell`：芽膨大期
2. `bud_burst`：萌芽期
3. `first_bloom`：初花期
4. `full_bloom`：盛花期
5. `petal_fall`：落瓣期
6. `fruit_set`：坐果期
7. `fruit_growth`：果实膨大期
8. `harvest`：采收期
9. `leaf_fall`：落叶期

完成季节志至少需要 `bud_burst`、`full_bloom`、`fruit_set`、`harvest` 四个阶段。比较时缺一方的阶段直接跳过，不补零，也不推断日期。

## 三条连通工作流

### 1. 园区与植株编目

入口为图谱台的“园区档案”工作区。用户创建园区草稿，添加至少一株植株，然后确认园区。服务端同时检查园区编号唯一性、植株编号唯一性、定植年份范围和状态转换条件。任何失败返回稳定错误码，且不写入部分数据。

### 2. 季节物候记录

入口为“物候观察”工作区。用户选择有效植株、年份和观察者，建立季节志；随后逐项补录阶段日期、置信度与说明；最后完成季节志。服务端验证阶段顺序、必需阶段、日期窗口和完成后的不可变约束。

### 3. 品种季节对齐

入口为“图谱比较”工作区。用户选择两份同年的已完成季节志，服务端计算共同阶段的相对日期偏移，拒绝缺少跨株共同阶段、年份不同或状态未完成的输入。比较结果保存为不可变记录。

补充流程：已确认园区可以生成只读编研简报，用于固定某个修订时点的档案内容。同一工作区还可以对同一植株发起多年比较：用户给定连续年份范围，服务端逐年给出纳入、排除或缺失的处置与原因，只在纳入年份上计算阶段统计，结果同样不可变，并可从列表、详情和导出文本中核对纳入标准。

## 模块边界

- `frontend/src/app`：应用壳、总览与跨工作区状态协调。
- `frontend/src/views`：园区档案、物候观察、图谱比较、编研简报四类工作区。
- `frontend/src/components`：阶段轨道、植株卡片、表单与反馈组件。
- `frontend/src/domain`：前端阶段字典、输入规则和展示模型。
- `frontend/src/services`：HTTP 客户端和统一错误映射。
- `backend/app/transport`：HTTP 路由、请求身份、严格 JSON 解析、幂等上下文与生命周期。
- `backend/app/application`：园区、观察、比较、简报用例编排。
- `backend/app/domain`：实体约束、状态转换和确定性计算。
- `backend/app/persistence`：SQLite 迁移、事务、实体版本、审计 outbox 和启动校验。
- `backend/app/security`：操作者、资源作用域、授权与撤销。
- `backend/app/jobs`：本地后台任务、租约、重试、死信和 worker。

## 接口约定

所有接口以 `/api` 开头，使用 JSON。成功响应直接返回资源对象或带 `items` 的集合。错误响应统一为：

```json
{
  "error": {
    "code": "validation_error",
    "message": "可读的中文错误",
    "details": {}
  }
}
```

写入接口支持 `revision` 字段进行乐观并发控制。修订号不匹配返回 `409 revision_conflict`，不会覆盖较新的记录。

## 状态和一致性规则

- 园区确认需要至少一株有效植株，确认后不可再次变更。
- 植株必须在所属园区仍为草稿时新增或变更。
- 季节志完成后不可修改；同一植株、同一年份只能有一个未取消的季节志，本基线不提供取消动作。
- 比较记录要求两侧季节志状态均为 `completed` 且年份相同。
- 多年比较要求范围内至少两个已完成年份；缺失年份不补零，排除年份不进入任何平均。
- 所有写入先通过领域校验，再在单个 SQLite 事务中提交业务实体、对象版本、审计事件和 outbox 事件。
- 相同操作者、相同幂等键和相同请求体会复用第一次成功响应；相同键复用不同请求会返回冲突。
- 请求通过 `X-Actor-Id` 识别操作者，并根据能力、资源类型和资源标识授权。
- 后台任务通过租约声明，支持尝试次数、延迟重试和死信状态。

## 视觉与交互

产品属于带现场工作属性的文化档案工具。首屏是实际编研工作面，而不是说明页。界面采用横向季节轨道、档案纸张式内容区和清晰的绿、赭、朱红状态语义；桌面端使用紧凑导航与并列工作面，窄屏改为纵向阶段轨道和大触控控件。所有可见文案使用简体中文。

## 构建、运行和验证

- 前端：Vue 3、TypeScript、Vite，输出静态资源。
- 后端：Python 标准库 HTTP 服务、SQLite 数据库和本地任务 worker。
- 构建命令执行严格类型检查、前端生产构建、Python 语法编译和基础测试。
- 三条有界工作流检查通过真实浏览器页面创建数据并验证服务端结果，使用临时数据目录，不访问互联网。
- 当前后端基础测试覆盖迁移、事务、并发写入、幂等、版本、审计、授权和任务生命周期；三条浏览器工作流继续覆盖真实页面链路。

## 当前范围外

不提供远程登录、密码或令牌体系、跨机器账号同步、外部气象数据、地图底图、移动端原生应用、数据删除和多节点部署。本机操作者与授权数据只用于本地工作流。简报为本机档案用途，不替代农业技术结论。

## 配对契约

成熟仓库与 `solo-0007-orchard-phenology-core` 使用相同的领域契约和路由语义。成熟仓库实现能力账本中标记为 `present` 的基础能力，核心仓库则保留对应缺口。
